<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.pjt1.model.dao.BoardDao" >

 	<insert id="insert"		parameterType="board"></insert> 
   	<delete id="delete" 	parameterType="int"> </delete>
   	<update id="update"		parameterType="board"></update>
   	<select id="search"		parameterType="int" resultType="board"> </select>
   	<select id="searchAll"	resultType="board">SELECT * FROM BOARD </select>
</mapper>

