<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.pjt1.demo.model.dao.FilesDao" >

  	<insert id="insert"		parameterType="files">
		INSERT INTO files
		(path_type, path_no, 
		files_name, files_thumbnail, 
		files_url)
		VALUES (
		#{path_type:INTEGER}, #{path_no:INTEGER},
		#{files_name:VARCHAR}, false,
		#{files_url:VARCHAR}}
		)
	</insert> 
   	<delete id="delete" 	parameterType="int"> </delete>

   	<update id="update"		parameterType="files">
		UPDATE files 
		SET
		path_type = #{path_no:INTEGER}, path_no=#{path_no:INTEGER}, 
		files_name=#{files_name:VARCHAR}, files_thumbnail=#{files_thumbnail:BOOLEAN},
		files_url=#{files_url:VARCHAR}, files_del_check=#{files_del_check:BOOLEAN}
		WHERE files_no =#{files_no:INTEGER}
	</update>
	
   	<select id="search"		parameterType="int" resultType="files">
		SELECT * 
		FROM files 
		WHERE files_no=#{files_no:INTEGER} 
		AND files_del_check=false   
	</select>
   	<select id="searchAll"	resultType="files">
		SELECT * 
		FROM files 
		WHERE files_del_check=false
	</select>
</mapper>

