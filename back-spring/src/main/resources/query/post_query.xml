<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.pjt1.demo.model.dao.PostDao" >
 	<select id="searchMorePostAll" parameterType="int" resultType="post">
        <![CDATA[
        SELECT * 
        FROM post 
        WHERE post_del_check = FALSE 
        ORDER BY post_regtime DESC LIMIT 0, #{perPageNum}]]>
    </select>
    <select id="searchMoreReport" parameterType="int" resultType="post">
        <![CDATA[
        SELECT * 
        FROM post 
        WHERE post_del_check = FALSE and post_type = 0
        ORDER BY post_regtime DESC LIMIT 0, #{perPageNum}]]>
    </select>
    <select id="searchMorePlan" parameterType="int" resultType="post">
        <![CDATA[
        SELECT * 
        FROM post 
        WHERE post_del_check = FALSE and post_type = 1
        ORDER BY post_regtime DESC LIMIT 0, #{perPageNum}]]>
    </select>
    <select id="searchMorePost" parameterType="int" resultType="post">
        <![CDATA[
        SELECT * 
        FROM post 
        WHERE post_del_check = FALSE 
        ORDER BY post_regtime DESC LIMIT 0, #{perPageNum}]]>
    </select>
     <select id="getCountPostAll" resultType="int">
        select count(*) from post where post_del_check =false
    </select>
    <select id="searchPagePostAll" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
        select * 
        from post 
        where post_del_check = FALSE
        order by post_regtime
        limit #{pageStart}, #{perPageNum}]]>
    </select>
    <select id="getCountReport" resultType="int">
        select count(*) from post where post_type = 0 and post_del_check =false
    </select>
    <select id="searchPageReport" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
        select * 
        from post 
        where post_del_check = FALSE and post_type = 0
        order by post_regtime
        limit #{pageStart}, #{perPageNum}]]>
    </select>
     <select id="getCountPlan" resultType="int">
        select count(*) from post where post_type = 1 and post_del_check =false
    </select>
    <select id="searchPagePlan" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
        select * 
        from post 
        where post_del_check = FALSE and post_type = 1
        order by post_regtime
        limit #{pageStart}, #{perPageNum}]]>
    </select>
  	<insert id="insert"		parameterType="post">
		INSERT INTO post
		(post_type, 
		board_no, 
		mem_no, 
		post_title, 
		post_content, 
		post_category, 
		post_city
		post_plan_start, 
		post_plan_end,
		post_plan_title)
		VALUES(
			#{post_type:INTEGER}, #{board_no:INTEGER}, #{mem_no:INTEGER},
			#{post_title:VARCHAR}, #{post_content:VARCHAR}, #{post_category:VARCHAR}, #{post_city:VARCHAR},
			#{post_plan_start:VARCHAR}, #{post_plan_end:VARCHAR}, #{post_plan_title:VARCHAR}
		)
	</insert> 
	
   	<delete id="delete" 	parameterType="int">
	   
	</delete>

   	<update id="update"		parameterType="post">
		UPDATE post 
		SET
		post_type=#{post_type:INTEGER}, 
		board_no=#{board_no:INTEGER}, 
		mem_no=#{mem_no:INTEGER},
		post_title=#{post_title:VARCHAR}, 
		post_content= #{post_content:VARCHAR}, 
		post_category=#{post_category:VARCHAR},
		post_regtime=CURRENT_TIMESTAMP(),
		post_city=#{post_city:VARCHAR},
		post_plan_start=#{post_plan_start:VARCHAR}, 
		post_plan_end=#{post_plan_end:VARCHAR}, 
		post_plan_title=#{post_plan_title:VARCHAR},
		post_plan=#{post_plan:VARCHAR},
		post_del_check=#{post_del_check:BOOLEAN}
		where post_no=#{post_no}
	</update>
	<update id="updatePostHitUp" parameterType="int">
		UPDATE post
		SET
		post_hits = post_hits + 1
		WHERE post_no=#{post_no}
	</update>
   	<select id="search"		parameterType="int" resultType="post">
		SELECT * 
		FROM post 
		WHERE 
		post_no = #{post_no:INTEGER} 
		AND post_del_check=false   
	</select>
	<!-- <resultMap type ="post" id="postMap">
		<id	column="post_no"	property="post_no"/>
		<result column="post_title" propet =/>
		<result/>
		<result/>
		<result/>
		<result/>
		<result/>
		<collection property="commentList" ofType="comment" ></collection>
	</resultMap> -->
	<!-- <select id="searchWithComment" parameterType ="int" resultType="postMap">
		select p.* , m.mem_id as cmt_person, c.cmt_no, c.mem_no, cmt_title, cmt_content             
		from   comment c                                              
		right join  (select post_no, post_title, mem_id, post_content, post_category                     
							, date_format(post_regtime, '%y-%m-%d') as post_regtime
							, post_hits, ((post_end_day)-(post_start_day)) as days
					 from   post, members                                        
					 where  post_no = #{post_no:INTEGER} and post.mem_no = members.mem_no) p                                   
		on   p.post_no = c.post_no
		left join members m
		on c.mem_no = m.mem_no;
	</select> -->
   	<select id="searchAll"	resultType="post">
		SELECT * FROM post WHERE post_del_check = false
	</select>
</mapper>

