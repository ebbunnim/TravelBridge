<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.pjt1.demo.model.dao.BoardDao" >

 	<insert id="insert"		parameterType="board">
		insert into board
		(board_name, board_del_check)
		values (
			#{board_name:VARCHAR},
			#{board_del_check:BOOLEAN}
		)
	</insert> 
   	<delete id="delete" 	parameterType="int">
		update board set board_del_check=true where board_no=#{board_no} and board_del_check=false
	</delete>

   	<update id="update"		parameterType="board">
	   	update board set
		board_name=#{board_name:VARCHAR}, board_del_check=#{board_del_check:BOOLEAN}
	</update>

   	<select id="search"		parameterType="int" resultType="board"> 
		select * from board where board_no=#{board_no:INTEGER} and board_del_check=false 
	</select>  

   	<select id="searchAll"	resultType="board">
		select * from board where board_del_check=false
	</select>
</mapper>

