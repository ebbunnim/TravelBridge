<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.pjt1.demo.model.dao.FilesDao" >

  	<insert id="insert"		parameterType="files">
		insert into files
		(post_no, files_name, files_thumbnail, files_url, files_del_check)
		values (
		#{post_no:INTEGER}, #{files_name:VARCHAR}, #{files_thumbnail:BOOLEAN},
		#{files_url:VARCHAR}, #{files_del_check:BOOLEAN}
		)
	</insert> 
   	<delete id="delete" 	parameterType="int"> </delete>

   	<update id="update"		parameterType="files">
		update files set
		post_no=#{post_no:INTEGER}, files_name=#{files_name:VARCHAR}, files_thumbnail=#{files_thumbnail:BOOLEAN},
		files_url=#{files_url:VARCHAR}, files_del_check=#{files_del_check:BOOLEAN}
	</update>
	
   	<select id="search"		parameterType="int" resultType="files">
		select * from files where files_no=#{files_no:INTEGER} and files_del_check=false   
	</select>
   	<select id="searchAll"	resultType="files">
		select * from files where files_del_check=false
	</select>
</mapper>

